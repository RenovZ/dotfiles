#set shell bash
#set shellopts '-eu'
#set ifs "\n"
set scrolloff 10
set sixel true

set ratios 1:2:3
set icons true

set previewer ~/.config/lf/preview
set cleaner ~/.config/lf/cleaner

# trash-cli
cmd trash %trash-put -- $fx

# node.js
cmd node_script ${{
    script="$(jq -r '.scripts | keys[]' <package.json | sort | fzf --height 20%)"
    npm run "$script"
}}

# zoxide
cmd z %{{
    result="$(zoxide query --exclude "$PWD" "$@" | sed 's/\\/\\\\/g;s/"/\\"/g')"
    lf -remote "send $id cd \"$result\""
}}

cmd zi ${{
    result="$(zoxide query -i | sed 's/\\/\\\\/g;s/"/\\"/g')"
    lf -remote "send $id cd \"$result\""
}}

# show git repo info on cd, file stats on select
cmd on-cd &{{
    if git rev-parse --git-dir >/dev/null 2>&1; then
        remote="$(git remote get-url origin 2>/dev/null | sed 's|.*[:/]\([^/]*/[^/.]*\)\(.git\)\?$|\1|')"
        branch="$(git branch --show-current)"
        commits="$(git rev-list --count HEAD 2>/dev/null)"
        authors="$(git shortlog -sne 2>/dev/null | wc -l | tr -d ' ')"
        [ -z "$remote" ] && remote="no remote"
        lf -remote "send $id set statfmt \"git: $remote | $branch | ${commits}c ${authors}a\""
    else
        lf -remote "send $id set statfmt \"\""
    fi
}}

# file stats via eza
cmd on-select &{{
    lf -remote "send $id set statfmt \"$(eza -ld --color=always --icons=always "$f" 2>/dev/null || ls -ld --color=always "$f" 2>/dev/null || ls -ld "$f")\""
}}

# git
cmd git_repo_info ${{
    if ! git rev-parse --git-dir >/dev/null 2>&1; then
        echo "Not a git repository."
        exit 0
    fi

    {
        # Header
        printf '\033[1;36m╭──────────────────────────────────────────────╮\033[0m\n'
        printf '\033[1;36m│\033[0m             \033[1;33mGit Repository Info\033[0m              \033[1;36m│\033[0m\n'
        printf '\033[1;36m╰──────────────────────────────────────────────╯\033[0m\n'
        echo

        # Remote
        remote_url="$(git remote get-url origin 2>/dev/null)"
        if [ -n "$remote_url" ]; then
            printf "\033[1;32m Remote:\033[0m    %s\n" "$remote_url"
        else
            printf "\033[1;32m Remote:\033[0m    \033[2m(no remote)\033[0m\n"
        fi

        # Branch
        branch="$(git branch --show-current)"
        if [ -z "$branch" ]; then
            branch="$(git rev-parse --short HEAD) (detached)"
        fi
        printf "\033[1;32m Branch:\033[0m    %s\n" "$branch"

        # Ahead/behind tracking
        if git rev-parse @{upstream} >/dev/null 2>&1; then
            ahead="$(git rev-list @{upstream}..HEAD --count)"
            behind="$(git rev-list HEAD..@{upstream} --count)"
            printf "\033[1;32m Tracking:\033[0m  "
            [ "$ahead" -gt 0 ] && printf "↑%s " "$ahead"
            [ "$behind" -gt 0 ] && printf "↓%s" "$behind"
            [ "$ahead" -eq 0 ] && [ "$behind" -eq 0 ] && printf "up to date"
            echo
        fi

        # Commits
        total_commits="$(git rev-list --count HEAD 2>/dev/null)"
        printf "\033[1;32m Commits:\033[0m   %s\n" "$total_commits"

        # Authors / Contributors
        authors="$(git shortlog -sne 2>/dev/null | wc -l | tr -d ' ')"
        printf "\033[1;32m Authors:\033[0m   %s\n" "$authors"

        # Tags
        tag_count="$(git tag | wc -l | tr -d ' ')"
        [ "$tag_count" -gt 0 ] && printf "\033[1;32m Tags:\033[0m      %s\n" "$tag_count"

        # First & last commit
        first_commit="$(git log --reverse --format='%ad (%h)' --date=short 2>/dev/null | head -1)"
        last_commit="$(git log -1 --format='%ad (%h)' --date=short 2>/dev/null)"
        printf "\033[1;32m First:\033[0m     %s\n" "$first_commit"
        printf "\033[1;32m Last:\033[0m      %s\n" "$last_commit"

        # Stashes
        stash_count="$(git stash list 2>/dev/null | wc -l | tr -d ' ')"
        [ "$stash_count" -gt 0 ] && printf "\033[1;32m Stashes:\033[0m   %s\n" "$stash_count"

        # Worktree status
        echo
        printf '\033[1;36m── Status ──\033[0m\n'
        git -c color.status=always status -s

        # Top contributors
        echo
        printf '\033[1;36m── Top Contributors ──\033[0m\n'
        git shortlog -sne 2>/dev/null | sort -rn | head -10
    } | less -R
}}

cmd git_add ${{clear;git-forgit add}}
cmd git_push ${{
    clear
    current_branch=$(git branch --show-current)
    selected=$(git branch -rv|fzf --header="$current_branch (current branch)
$(git --no-pager remote -v|awk '{print $1 " " $2}'|sort|uniq)" \
    --preview="echo {}|awk '{print \$2}'|xargs git show")
    remote=$(echo $selected|awk -F / '{print $1}')
    target_branch=$(echo $selected|awk '{print $1}'|awk -F / '{print $2}')
    if [ ! -z $remote ]; then
        echo "git push $remote $current_branch:$target_branch"
        git push $remote $current_branch:$target_branch
    fi
}}
cmd git_diff ${{clear;git-forgit diff}}
cmd git_pull ${{
    clear
    # remote=$(git --no-pager remote -v | fzf | awk '{print $1}')
    # branch=$(git --no-pager branch -vv | fzf | sed "s/^* //" | awk '{print $1}')
    current_branch=$(git branch --show-current)
    selected=$(git branch -rv|fzf --header="$current_branch (current branch)
$(git --no-pager remote -v|awk '{print $1 " " $2}'|sort|uniq)" \
    --preview="echo {}|awk '{print \$2}'|xargs git show")
    remote=$(echo $selected|awk '{print $1}'|awk -F / '{print $1}')
    branch=$(echo $selected|awk '{print $1}'|awk -F / '{print $2}')
    if [ ! -z $remote ]; then
        echo "git pull $remote $branch"
        git pull $remote $branch
    fi
}}
cmd git_status ${{clear; git -c color.status=always status | less -R}}
cmd git_reset_head ${{clear;git-forgit reset_head}}
cmd git_log ${{clear;git-forgit log}}
cmd git_remote ${{clear; git remote -v | less -R}}
cmd git_checkout_branch ${{clear;git-forgit checkout_branch}}
cmd git_branch_delete ${{clear;git-forgit branch_delete}}
cmd git_checkout_tag ${{clear;git-forgit checkout_tag}}
cmd git_checkout_commit ${{clear;git-forgit checkout_commit}}
cmd git_checkout_file ${{clear;git-forgit checkout_file}}
cmd git_blame ${{clear;git-forgit blame}}
cmd git_stash_show ${{clear;git-forgit stash_show}}
cmd git_commit ${{
    clear
    git commit "$@"
}}
cmd git_fixup ${{clear;git-forgit fixup}}
cmd git_cherry_pick ${{clear;git-forgit cherry_pick}}
cmd git_ignore ${{clear;git-forgit ignore}}
cmd git_show ${{clear;git-forgit show}}

map gso :git_show
map ga :git_add
map gp :git_push
map gd :git_diff
map gu :git_pull
map gl :git_log
map ge push %git<space>
map gi :git_ignore
map gce :git_commit
map gbd :git_branch_delete
map gcb :git_checkout_branch
map gct :git_checkout_tag
map gcc :git_checkout_commit
map gcp :git_cherry_pick
map gcf :git_checkout_file
map gbl :git_blame
map gst :git_status
map gss :git_stash_show
map gfu :git_fixup
map grv :git_remote
map grh :git_reset_head
map gI :git_repo_info

# fzf
cmd fzf $$EDITOR $(find . -name "$1" | fzf)
cmd fzf_jump ${{
    res="$(fd --type d \
        --hidden \
        --no-ignore \
        --exclude '.git' \
        --ignore-case \
        | fzf --header='Jump to location' | sed 's/\\/\\\\/g;s/"/\\"/g')"
    if [ -d "$res" ] ; then
        cmd="cd"
    elif [ -f "$res" ] ; then
        cmd="select"
    else
        exit 0
    fi
    lf -remote "send $id $cmd \"$res\""
}}
# cmd rg_search $~/.config/lf/rg_search.sh
cmd rg_search ${{
    res="$( \
        RG_PREFIX="rg -U --column \
            --hidden \
            --line-number \
            --no-heading \
            --glob '!.git/*' \
            --color=always \
            --smart-case "
        preview="
            # echo $SHELL >> /tmp/lf.log
            filepath=\$(echo {}|awk -F ':' '{print \$1}')
            currentline=\$(echo {}|awk -F : '{print \$2}')
            startline=\$([ \$currentline -ge 20 ] && expr \$currentline - 20 || expr 0)
            # endline=\$(expr \$currentline + 10)
            # bat -n --line-range \$startline:\$endline --highlight-line \$currentline --color always \$filepath
            bat -n --line-range \$startline: --highlight-line \$currentline --color always \$filepath
        "
        FZF_DEFAULT_COMMAND="$RG_PREFIX ''" \
            fzf --bind "change:reload:$RG_PREFIX {q} || true" \
            --ansi --header 'Search in files' \
            --preview "$preview" \
            | cut -d':' -f1
    )"
    [ ! -z "$res" ] && lf -remote "send $id select \"$res\""
}}
cmd fd_search ${{
    res="$( \
        FD_PREFIX="fd --color=always \
            --type f \
            --exclude '.git' \
            --ignore-case "
        preview="bat --color=always --style=numbers {}"
        FZF_DEFAULT_COMMAND="$FD_PREFIX ''" \
            fzf --bind "change:reload:$FD_PREFIX {q} || true" \
            --ansi --header 'Search in files' \
            --preview "$preview" \
            | cut -d':' -f1
    )"
    [ ! -z "$res" ] && lf -remote "send $id select \"$res\""
}}
cmd fdh_search ${{
    res="$( \
        FD_PREFIX="fd --color=always \
            --hidden \
            --type f \
            --exclude '.git' \
            --ignore-case "
        preview="bat --color=always --style=numbers {}"
        FZF_DEFAULT_COMMAND="$FD_PREFIX ''" \
            fzf --bind "change:reload:$FD_PREFIX {q} || true" \
            --ansi --header 'Search in files' \
            --preview "$preview" \
            | cut -d':' -f1
    )"
    [ ! -z "$res" ] && lf -remote "send $id select \"$res\""
}}
cmd fdi_search ${{
    res="$( \
        FD_PREFIX="fd --color=always \
            --no-ignore \
            --type f \
            --exclude '.git' \
            --ignore-case "
        preview="bat --color=always --style=numbers {}"
        FZF_DEFAULT_COMMAND="$FD_PREFIX ''" \
            fzf --bind "change:reload:$FD_PREFIX {q} || true" \
            --ansi --header 'Search in files' \
            --preview "$preview" \
            | cut -d':' -f1
    )"
    [ ! -z "$res" ] && lf -remote "send $id select \"$res\""
}}
cmd fda_search ${{
    res="$( \
        FD_PREFIX="fd --color=always \
            --hidden \
            --no-ignore \
            --type f \
            --exclude '.git' \
            --ignore-case "
        preview="bat --color=always --style=numbers {}"
        FZF_DEFAULT_COMMAND="$FD_PREFIX ''" \
            fzf --bind "change:reload:$FD_PREFIX {q} || true" \
            --ansi --header 'Search in files' \
            --preview "$preview" \
            | cut -d':' -f1
    )"
    [ ! -z "$res" ] && lf -remote "send $id select \"$res\""
}}
cmd fzf_raw ${{
    # preview="bat --color=always --style=numbers --line-range=:500 {}"
    fzf
}}

cmd fzf_search ${{
    cmd="rg --column --line-number --no-heading --color=always --smart-case"
    fzf --ansi --disabled --layout=reverse --header="Search in files" --delimiter=: \
        --bind="start:reload([ -n {q} ] && $cmd -- {q} || true)" \
        --bind="change:reload([ -n {q} ] && $cmd -- {q} || true)" \
        --bind='enter:become(lf -remote "send $id select \"$(printf "%s" {1} | sed '\''s/\\/\\\\/g;s/"/\\"/g'\'')\"")' \
        --preview='bat --color=always --style=numbers --highlight-line={2} -- {1}' \
        --preview-window='+{2}-/2'
}}

map f
map fg :fzf_search
map fj :fzf_jump
map fs :rg_search
map fd :fd_search
map fh :fdh_search
map fi :fdi_search
map fa :fda_search
map fe $$EDITOR $(fzf)
map fw :fzf_raw
map ff push :fzf<space>

map . set hidden!

#create new directory or file
map o
cmd mkdir %mkdir "$@"
map od push %mkdir<space>
cmd touch %touch "$@"
map of push %touch<space>

#delete file or directories
map x
map x push :delete<enter>

map i $~/.config/lf/preview $f | less -R
